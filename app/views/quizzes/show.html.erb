<!--SESに騙されやすい診断-->
<!--
<h1>SESについてのクイズ</h1>
<p> クイズ作成中。</p>
-->

<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">SESに騙されやすい度チェック</h1>
  <p>全部で10問！基本的なクイズに答えるだけで、あなたが悪徳SESに騙されやすいかどうかチェックします！</p>
  
  <%= form_with url: ses_quiz_path, method: :post, local: true, class: "space-y-8" do |f| %>
    <% @questions.each_with_index do |question, index| %>
      <div class="bg-white shadow-md rounded-lg p-6">
        <h2 class="text-xl font-semibold mb-4">質問 <%= index + 1 %>: <%= question.content %></h2>
        <div class="space-x-4">
          <%= f.radio_button "answers[#{question.id}]", true, class: "mr-2" %>
          <%= f.label "answers[#{question.id}]_true", "はい", class: "mr-4" %>
          <%= f.radio_button "answers[#{question.id}]", false, class: "mr-2" %>
          <%= f.label "answers[#{question.id}]_false", "いいえ", class: "mr-4" %>
        </div>
      </div>
    <% end %>
    
    <%= f.submit "結果を見る", data: { turbo: false }, class: "mt-6 bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded" %>
  <% end %>
</div>
